<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core sensors -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Function-call tools (network) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Always-on wake-word service runs as a foreground microphone service.
         POST_NOTIFICATIONS is needed on API 33+ for the service notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Bluetooth HID for 8BitDo Micro gamepad -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- system_action tool dispatch surfaces.
         NB: SMS/email/call/calendar use ACTION_SENDTO / ACTION_DIAL /
         ACTION_INSERT — these need no runtime permission. We declare only
         the AlarmClock permission (the official package-namespaced one). -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".app.GemmaVisionApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.GemmaVision2"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Always-on wake-word listener (Porcupine) -->
        <service
            android:name=".audio.WakeWordService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- Cooperative accessibility service to read foreground-app context.
             We DO NOT drive UI from here. Only used for richer prompt context. -->
        <service
            android:name=".accessibility.GemmaVisionA11yService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
